import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /**
   * 允许内网其他设备访问 dev 服的 HMR/静态资源。
   * Next.js 16 默认只放行 localhost；跨设备访问会被拦（表现：页面能打开但按钮无响应）。
   * 这里放行本机所有 10.x / 192.168.x 内网网段，方便同 Wi-Fi 同事访问。
   * 生产（vercel）不受影响。
   */
  allowedDevOrigins: [
    '10.31.169.75',           // 当前本机内网 IP（办公网）
    '*.local',                 // mDNS 主机名（如 macbook.local）
    '10.*',                    // 全部 10.x.x.x 网段（办公网常用）
    '192.168.*',               // 家用 Wi-Fi 网段
    '172.16.*', '172.17.*', '172.18.*', '172.19.*',
    '172.20.*', '172.21.*', '172.22.*', '172.23.*',
    '172.24.*', '172.25.*', '172.26.*', '172.27.*',
    '172.28.*', '172.29.*', '172.30.*', '172.31.*',
  ],
};

export default nextConfig;
